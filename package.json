{
  "name": "infra",
  "version": "1.0.0",
  "description": "Install serverless packages",
  "main": "index.js",
  "scripts": {
    "deploy": "serverless --host=ci --stage=${STAGE} deploy --alias provisioned"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serverless": "^2.23.0",
    "serverless-plugin-aws-alerts": "^1.6.1",
    "update-template": "^1.3.1"
  }
}
